<li class = "list">
  <%= image_tag post.image, class: "post-img" %>
  <h3 class='post-title'>
    <%= post.title %>
    <span class = "like-box">
    <% if current_user.liked_by?(post.id) %>
      <%= link_to 'いいねを外す', destroy_like_path(post.id), method: :DELETE, remote: true %><%= post.likes.count %>
    <% else %>
      <%= link_to 'いいね', create_like_path(post.id), method: :POST, remote: true %><%= post.likes.count %>
    <% end %>
    </span>
  </h3>
  <%= link_to post_path(post.id) do %>
    <p class = "root-show">〜クリックして詳細を見る〜</p>
  <% end %>
</li>